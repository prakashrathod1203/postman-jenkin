pipeline {
    agent any

    stages {
        stage('Running tests') {
            steps {
                echo 'Testing'
                sh "newman run ./collection-1.json"
            }
        }
    }
}
